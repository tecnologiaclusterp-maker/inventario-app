## Packages
date-fns | Used for formatting dates in Spanish

## Notes
- Using Shadcn UI components that are already installed
- Relying on Replit Auth (`/api/login`, `/api/auth/user`)
- File uploads use standard `FormData` and POST to `/api/uploads`
- App defaults to dark mode styling for a premium, professional look
